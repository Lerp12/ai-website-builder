[project]
name = "ai-web-backend"
version = "0.1.0"
description = "Minimal AI website builder backend"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "openai>=1.55.0",
    "python-dotenv>=1.0.1",
    "beautifulsoup4>=4.12.0",
]

[tool.uv]
package = false
